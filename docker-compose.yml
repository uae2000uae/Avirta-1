version: '3'
services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
    restart: always
    volumes:
      - ./contents:/app/contents