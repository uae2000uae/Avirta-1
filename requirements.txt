# Avirta Project Runtime Dependencies (Cloud Run optimized)

# Web Framework
flask==2.3.2
jinja2==3.1.2
requests==2.31.0
nltk==3.8.1

google-cloud-secret-manager>=2.20.0

# Production WSGI server
gunicorn>=22.0.0

# Note:
# - Removed functions-framework (not needed when running as a container on Cloud Run)
# - Removed dev-only tools (black, flake8) to keep image small